# agents-dir fixture

This directory is used to test that AGENTS.md beats verevoir-mcp.json and aigency.json.

```verevoir-mcp
{ "notion": { "databases": { "work_tracker": "agents-tracker-id" } } }
```
