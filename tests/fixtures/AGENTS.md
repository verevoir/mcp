# Project context

This is a test AGENTS.md with a verevoir-mcp block used by the explicit `--manifest` test.

```verevoir-mcp
{ "notion": { "databases": { "work_tracker": "explicit-agents-tracker-id" } } }
```
